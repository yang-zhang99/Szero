server:
  port: 28220
  servlet:
    context-path: /tdmo
management:
  server:
    port: 28221
  endpoints:
    web:
      exposure:
        include: '*'
spring:
  profiles:
    active: ${SPRING_PROFILES_ACTIVE:default}